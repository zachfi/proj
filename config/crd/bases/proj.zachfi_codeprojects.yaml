---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: codeprojects.proj.zachfi
spec:
  group: proj.zachfi
  names:
    kind: CodeProject
    listKind: CodeProjectList
    plural: codeprojects
    singular: codeproject
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: CodeProject is the Schema for the codeprojects API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CodeProjectSpec defines the desired state of CodeProject
            properties:
              owner:
                description: Owner is the github project owner.
                type: string
              post_template_hooks:
                description: PreTemplateHooks are executed in order after the templates
                  are written to the working directory of the project.
                items:
                  properties:
                    args:
                      items:
                        type: string
                      type: array
                    command:
                      type: string
                  type: object
                type: array
              pre_template_hooks:
                description: PreTemplateHooks are executed in order before the templates
                  are written to the working directory of the project.
                items:
                  properties:
                    args:
                      items:
                        type: string
                      type: array
                    command:
                      type: string
                  type: object
                type: array
              repo:
                description: Owner is the github repo name.
                type: string
              template_volumes:
                description: TemplateVolumes creates the source directory structure
                  of templates based on configmaps, mounted to specific relative locations.  This
                  final directory structure will be used as the `--input-dir` argument
                  for `gomplate`.
                items:
                  properties:
                    configmap:
                      type: string
                    mount_path:
                      type: string
                  type: object
                type: array
            type: object
          status:
            description: CodeProjectStatus defines the observed state of CodeProject
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
